import 'package:flutter/material.dart';
import 'package:hello_world/app_widget.dart';

//Para funcionar, o main.dart tem q ter o método main, obviamente
main() {
  /*Além disso, precisamos também utilizar um método chamado runApp(),
  que recebe o que ele irá rodar
  No caso, ele está rodando a class MyApp que está em outro arquivo*/
  runApp(MyApp());
}
  